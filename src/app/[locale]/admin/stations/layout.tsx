import { ReactNode } from 'react';

export const metadata = {
  title: 'Camper Intelligence - Admin Stations',
};

export default function AdminStationsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
