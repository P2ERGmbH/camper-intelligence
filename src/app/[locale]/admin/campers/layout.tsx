import { ReactNode } from 'react';

export const metadata = {
  title: 'Camper Intelligence - Admin Campers',
};

export default function AdminCampersLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
