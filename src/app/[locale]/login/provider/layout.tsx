import { ReactNode } from 'react';

export const metadata = {
  title: 'Camper Intelligence - Provider Login',
};

export default function ProviderLoginLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
