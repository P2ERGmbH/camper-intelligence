import { ReactNode } from 'react';

export const metadata = {
  title: 'Camper Intelligence - Contact Us',
};

export default function ContactLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
